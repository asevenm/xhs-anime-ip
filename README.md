# 小红书动漫 IP 自动发布系统

每日自动生成新海诚风格的小红书壁纸内容

## 🚀 功能

- **planner.py**: 使用 Gemini AI 生成每日内容策划（标题、正文、图片 prompt）
- **painter.py**: 使用 Gemini 图像生成 6 张壁纸
- **publisher.py**: 通过 Playwright 发布到小红书

## 📦 安装

```bash
pip install -r requirements.txt
python -m playwright install chromium
```

## ⚙️ 配置

创建 `.env` 文件：

```env
GEMINI_API_KEY=your_gemini_api_key
```

## 🎯 使用方式

### 手动运行

```bash
# 1. 生成内容策划
python planner.py

# 2. 生成图片
python painter.py

# 3. 发布到小红书（会打开浏览器）
python publisher.py
```

### GitHub Actions 自动化

1. 在仓库 Settings → Secrets 添加 `GEMINI_API_KEY`
2. 每天北京时间 8:00 自动生成内容
3. 内容生成后，本地运行 `python publisher.py` 发布

## 📁 目录结构

```
├── planner.py           # 内容策划
├── painter.py           # 图片生成
├── publisher.py         # 小红书发布
├── content/             # 生成的内容
│   └── 2024-01-01/
│       ├── meta.json    # 标题、正文、标签
│       ├── 1.png        # 图片 1-6
│       └── ...
└── .github/workflows/   # GitHub Actions
```

## 🎨 内容风格

- **新海诚美学**：极致光影 + 超写实壁纸感
- **支持任意动漫 IP**：优先同系列角色，保持统一性
- **6 张图片**：不同场景（天台、雨中城市、星空等）
